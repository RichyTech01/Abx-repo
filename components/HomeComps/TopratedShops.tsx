import { View, Text } from 'react-native'
import SectionHeader from '@/common/SectionHeader'
import React from 'react'

export default function TopratedShops() {
  return (
    <View>
      <SectionHeader title='Top rated shops' onPress={() => {}} />
    </View>
  )
}